name: test

on: push
env:
  info: yes


jobs:
    first:
        
      runs-on: ubuntu-latest
      steps:
      - name: checkout
        uses: actions/checkout@v4
   

      - name: ls
        run: |
               cat README.md
               echo -e "\n"
               echo ${{env.info}}

  
      - name: produce_output
        id: ids
        run: echo "value=TEST" >> $GITHUB_OUTPUT

      - name: collect_output
        run: echo "The ouput is ${{steps.ids.outputs.valulle}}"

        
    second:
      runs-on: ubuntu-latest
  
      needs: first
      steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          repository: zoundibona/git_test
          token: ${{secrets.token}}
      
      - name: list dir
        run: |

             var=$(cat file.txt | grep image)
             echo $var | tee file.txt
             sed -i s/ajoutlines/line/g file.txt
             git config --global user.email "zoundibona@yahoo.fr"
             git config --global user.name "zoundibona"
             git add file.txt
             git commit -m updatefile
             git push
             



   


      
        

